import { StubPage } from "../(stubs)/StubPage";
export default function SettingsAdmin() {
  return <StubPage title="Settings" blurb="WhatsApp number, Instagram URL, hero copy, stats (Phase 2)." />;
}
